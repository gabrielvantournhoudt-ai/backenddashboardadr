services:
  - type: web
    name: adrs-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python backend_real.py
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"


